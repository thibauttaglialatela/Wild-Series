{% extends "base.html.twig" %}

{% block body %}
    <h1 class="text-light">Catégories</h1>
    {% for category in categories %}
    <a href="/category/{{ category.name }}"><h2 class="text-light">{{ category.name }}</h2> </a>
           
    {% endfor %}
{% endblock  %}