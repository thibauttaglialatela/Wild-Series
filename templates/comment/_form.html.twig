{{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.comment) }}
    {{ form_row(form.rate) }}

<button class="btn btn-info">{{ button_lable|default('Sauvegarder')|trans }}</button>
{{ form_end(form) }}
