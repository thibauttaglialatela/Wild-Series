{% extends "base.html.twig" %}
{% block body %}
    {{ form_start(form) }}
    {{ form_errors(form) }}
    <div class="row">
        <div class="col text-light">
            {{ form_widget(form) }}
        </div>
    </div>
    <button type="submit" class="btn btn-danger">{{ 'Ajouter une série'|trans }}</button>
    {{ form_end(form) }}

{% endblock %}
