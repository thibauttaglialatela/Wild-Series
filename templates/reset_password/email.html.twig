<h1>{{ 'Salut'|trans }}</h1>

<p>{{ 'Pour réinitialiser votre mot de passe veuillez suivre le lien suivant : '|trans }}</p>

<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

<p>{{ "Ce lien expirera dans"|trans }} {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}
    .</p>

<p>{{ 'Cordialement'|trans }}</p>
