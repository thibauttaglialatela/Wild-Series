{% extends "base.html.twig" %}
{% block title %}
    {{ 'Editer un acteur'|trans }}
{% endblock %}

{% block body %}
    <h1 class="text-light text-center">{{ 'Editer un acteur'|trans }}</h1>

    {{ include('actor/_form.html.twig', {'button_label': 'Mettre à jour'|trans}) }}

    <a href="{{ path('actor_index') }}">{{ "Revenir à la liste des acteurs"|trans }}</a>

    {{ include('actor/_delete_form.html.twig') }}
{% endblock %}
