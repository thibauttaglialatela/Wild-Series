{% extends 'base.html.twig' %}

{% block title %}New Actor{% endblock %}

{% block body %}
    <h1 class="text-light text-center">Create new actor</h1>

    {{ include('actor/_form.html.twig') }}

    <a class='text-light' href="{{ path('actor_index') }}">back to list</a>
{% endblock %}
