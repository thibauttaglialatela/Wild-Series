<form method="post" action="{{ path('actor_delete', {'id': actor.id}) }}"
      onsubmit="return confirm({{ 'Etes vous sûre de vouloir supprimer la fiche de cet acteur ?'|trans }});">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actor.id) }}">
    <button class="btn btn-danger btn-lg">{{ 'Supprimer la fiche'|trans }}</button>
</form>
