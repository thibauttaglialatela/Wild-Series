{% extends 'base.html.twig' %}

{% block title %}{{ 'Editer une saison'|trans }}{% endblock %}

{% block body %}
    <h1 class="text-light">{{ 'Editer une saison'|trans }}</h1>

    {{ include('season/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="text-light" href="{{ path('season_index') }}">{{ 'Revenir à la liste'|trans }}</a>

    {{ include('season/_delete_form.html.twig') }}
{% endblock %}
